//to print the area of square using input function
#include <stdio.h>
int main()
{
        int a,area;
        printf("enter the lengths of square: ");
        scanf("%d",&a);
        area=a*a;
        printf("the area of square is: %d" , area);
        return 0;
}

