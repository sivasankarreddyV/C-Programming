//to print the perimeter of square using input function
#include <stdio.h>
int main()
{
	int a,perimeter;
	printf("enter the length of a rectangle: ");
	scanf("%d",&a);
	perimeter=4*a;
	printf("the perimeter of square is: %d" , perimeter);
	return 0;
}
